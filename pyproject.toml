[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "atelierb-mcp"
version = "0.1.0"
description = "MCP Server for Atelier B - Integrating Claude AI with formal methods"
readme = "README.md"
license = "AGPL-3.0-or-later"
requires-python = ">=3.11"
authors = [
    { name = "Thierry Lecomte", email = "thierry.lecomte@clearsy.com" }
]
keywords = ["mcp", "atelier-b", "formal-methods", "b-method", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    # Upper bound is load-bearing: mcp 2.0 removed the low-level Server API this
    # server is built on (@server.list_tools / @server.call_tool / stdio_server).
    # Without it, a fresh install silently resolves to 2.x and the server dies on
    # import, which Claude Code reports only as "no tools" (2026-08-03).
    "mcp>=1.0.0,<2",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "mypy>=1.8.0",
    "ruff>=0.2.0",
]

[project.scripts]
atelierb-mcp = "atelierb_mcp.server:main"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-v"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
